name: Frontend CI/CD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Remove npm cache and reinstall
        run: |
          npm cache clean --force
          rm -rf package-lock.json node_modules
          npm install

      - name: Build
        run: npm run build
